#pragma once

#include "NetworkSimulator.hpp"

/**
 * Indicates an event at a node at which that node should start sensing
 * the channel as being busy.
 */
class ChannelBusyStartEvent : NetworkEvent
{
public:

};